upstream backend  {
  server 127.0.0.1:8081;
}

server {
  listen   80;
  server_name  <%= node['hostname'] %>;
  access_log  <%= node['nginx']['log_dir'] %>/node.access.log;

  location / {
    proxy_pass           http://backend;
  }
}

